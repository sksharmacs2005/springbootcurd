server:
  port: 8081

spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

  datasource:
       url: jdbc:mysql://localhost:3306/demo
       username: root
       password:
  jpa:
       hibernate:
          ddl-auto: update
       show-sql: true
       generate-ddl: true


